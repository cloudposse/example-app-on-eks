# Ordered list of releases.
environments:
  default:
  preview:
  staging:
  production:

helmfiles:
#  - "releases/pull-secret.yaml"
  - path: "releases/app.yaml"
    values:
      - test: test
        {{ .Values | toYaml }}
#  - "releases/istio.yaml"
